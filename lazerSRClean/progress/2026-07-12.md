# 2026-07-12

- osu!lazer 2026.624.0→2026.711.0 업데이트 후 lazerSR 대부분 기능 미작동 — 원인 조사 시작
- `HookLog.cs` 실제 파일 로깅 구현(임시), `StartupHook.cs`/`Patcher.cs`에 진단 로그 추가(임시, PatchAll 사전 probe + LoaderException 상세 로깅)
- GitHub `ppy/osu` 624 태그(8c3c1b5) ↔ 711 태그(1164870d) 커밋 diff로 원인 확정 (서브에이전트 활용)
- 버그 A 확정: `DifficultyCalculator.CreateDifficultyAttributes/CreateSkills/CreateDifficultyHitObjects`에서 `clockRate` 파라미터 삭제됨 → `ManiaDifficultyCalculator`가 상속 중이라 `TypeLoadException`
- 버그 B 확정: `StrainSkill.ObjectStrains`/`GetObjectStrains()` 삭제, `Skill.ObjectDifficulties`로 대체됨 → `Strain`이 상속 중이라 `MissingMethodException`
- 버그 C 확정: `osu.Game.Rulesets.Difficulty.Utils.DifficultyCalculationUtils` → `DiffUtils`로 이름 변경됨 → `TypeLoadException`
- `Difficulty/ManiaDifficultyCalculator.cs` 수정: `DifficultyCalculator` 상속 제거, `Calculate()`/`CalculateTimed()` 루프 독립 구현
- `Difficulty/Skills/Strain.cs` 수정: `StrainSkill` 상속 제거, 자체 `objectStrains` 리스트로 대체 (미사용 section-peak 로직 삭제)
- `Difficulty/Utils/DifficultyValueUtils.cs`에 `Smoothstep`/`Logistic` 이식, `StrainEvaluator.cs`/`ReleasePreprocessor.cs` 호출부 변경 — osu! `DifficultyCalculationUtils`/`DiffUtils` 의존 완전 제거
- 사용자 실기 테스트로 버그 A/B/C 전부 해결 확인
- 진단 로그 코드(`HookLog.cs`/`StartupHook.cs`/`Patcher.cs`) 원상복구
- `installer/LazerSRClean.iss` 버전 4.0.0→4.1.0, dotnet publish + Inno Setup 컴파일, `LazerSR-v4.1.0-Setup.exe` 생성 및 전달
- 서브에이전트 3개 병렬로 "osu! live 클래스 의존/상속" + "safety-guidelines.md 레드라인" + "스레드·메모리 패턴" 전수 감사 (수정 없음, 조사만)
- 감사 결과 기록 (패치 안 함, 추후 참고용):
  - `ManiaDifficultyHitObject : DifficultyHitObject` — 버그 A/B와 동일한 미발현 상속 위험 (HIGH)
  - `ReplayScoreTimeline.cs` — osu! `ManiaScoreProcessor`/`HitWindows`/`TailNote` 등 채점 내부 클래스 직접 구동 (MEDIUM)
  - `ModUtils.CalculateRateWithMods` 정적 호출 3곳 (MEDIUM)
  - `PlayerGameplayPatch.cs` — `Player.LoadComplete` 패치 + 게임플레이 중 100ms마다 실시간 점수/콤보를 파이프로 전송 + `.osr` 리플레이 직접 파싱 (safety-guidelines.md 레드라인 다중 위반)
  - `BeatmapMetadataDisplayPatch`/`SkinWidgetRegistrarPatch`/`ExpandedPanelMiddleContentPatch`/`BeatmapAttributesDisplayPatch`/`DifficultyIconTooltipPatch` — 문서 기준(`Select.*` 네임스페이스 + `LoadComplete`)에서 벗어난 타겟
  - `SkinWidgetRegistrarPatch`가 등록하는 6개 스킨 위젯이 게임플레이 중 `GameplayState.ScoreProcessor` 등 라이브 상태를 직접 읽음
  - `DifficultyIconTooltipPatch`의 `static bool _pillInserted` — 세션 최초 1회만 pill 삽입되는 실사용 버그 (CONFIRMED)
  - `ConditionalWeakTable`/`PatchState` 안전망이 `lazerSRClean`엔 아예 없음 — 설계문서(`ui-patch-guide.md` 등)와 실제 구조 불일치 (지금은 우연히 안전)
