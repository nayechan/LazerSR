# 2026-08-19

## sunny+ 격리 계층

- ~~`SunnyConstants` 필드를 static field → `AsyncLocal` 백업 프로퍼티~~ 로 재구성. `SunnyConstants.WithIsolatedDiff(deltas, action)`으로 감싼 콜 컨텍스트 안에서만 다른 상수 세트가 읽히고, 프로세스 전역 기본값(만인diff만)은 절대 안 건드림 — 개인화diff 굽기/적용이 다른 sunny 소비처와 레이스 없이 공존
- ~~`DiffCombiner`~~ 주석 갱신: 개인화diff는 이 경로로 안 합쳐지고 `WithIsolatedDiff`로만 적용된다는 확정 방향 반영

## 개인화 diff 파이프라인 (`Tuning/`, `Hook/PersonalSunny/`)

- ~~`PersonalBox.cs`~~ 신규 — 개인화 대상 11개 상수, 만인 지점 중심 ±30% 박스/스텝 계산
- ~~`PersonalJacobianBaker.cs`~~ 신규 — 맵 1개당 23회 스윕(기준 1 + 11×2 중심차분)으로 SR0 + 유닛공간 자코비안 굽기, `WithIsolatedDiff`로 격리
- ~~`PersonalFitSolver.cs`~~ 신규 — `personal_fit.py`의 ridge solve 포팅 (13×13 가우스 소거, 3패스 beta 보정, α·β 보존). **버그 수정**: 성공 경로에서 마지막 ridge 반복을 안 거친 `alpha0`을 반환하던 것을 최종 `coef[0]`으로 정정 (96% 예상 sunny 값이 비정상적으로 낮게 나오던 원인)
- ~~`PersonalDiff.cs`~~ 컴파일타임 0벡터 → 런타임 가변 상태(`Update`/`CombinedWithUniversal`)로 변경
- ~~`Hook/PersonalSunny/`~~ 신규 폴더 — 큐/J캐시/적합결과 3파일 독립 저장(`%LocalAppData%\LazerSR\personalsunny\`), NM/DT/HT/NC/DC/HO/IN 화이트리스트, `PersonalSunnyService`(큐→굽기→적합→반영 파이프라인, 상태는 위젯이 폴링)
- ~~`Patches/PersonalSunnyScoreCollectorPatch.cs`~~ 신규 — `Player.ImportScore` Postfix로 실제 개인 스코어 생성 시 자동 큐잉(훈련/구간연습/리플레이감상은 `base` 호출 안 해서 자동으로 제외됨). 읽기 전용
- ~~`Widgets/PersonalSunnyWidget.cs`~~ 신규, `SkinWidgetRegistrarPatch`에 등록 — 선곡 화면 전용. 상단 pill 2개(현재 맵 개인화 sunny / 정확도 96% 예상 sunny), 하단 굽기 진행률 막대 ↔ 큐개수+수집버튼
- 이 PC 실기 리플레이가 없어 임시 픽스처(`PersonalSunnyDevFixture.cs`, `C:\dev\OsuScoreModel` 기록 사용)로 파이프라인 전체 검증 후 원복·삭제 완료. 이 과정에서 날짜 파싱 버그(`AssumeUniversal` → Kind=Local → `DateTimeOffset` 생성자 예외, 로컬 매칭될 때마다 조용히 파이프라인 중단)도 잡아서 고침 — 이건 임시 코드였으므로 fix 자체도 같이 삭제됨

## StrainGraphWidget 개인화 오버레이

- ~~`StrainGraphWidget.cs`~~ 만인/만인+개인 strain을 각각 계산해 `StrainAreaGraph.Update(universal, personal, honeySpots2)`로 전달
- ~~`Drawables/StrainAreaGraph.cs`~~ `StrainCapCurve` 신규 — [Low,High] 구간만 뜬 막대 렌더링. 교집합(그대로 흰/회색), 만인>개인(파랑, 내가 남들보다 잘 치는 부분), 개인>만인(빨강, 못 치는 부분) 3색 오버레이. 정규화 없이 원시값 비교, 화면 높이 맞춤용 분모만 두 곡선 공유

## 배포

- v6.3.0 (`installer\output\LazerSR-v6.3.0-Setup.exe`) — sunny+ 격리 계층 + 개인화 diff 파이프라인/위젯 + StrainGraph 오버레이 포함

## Git

- `C:\dev\lazerSR`에 git 저장소 초기화, `.gitignore` 작성(`bin/`·`obj/`·`installer/output/` 재생성 가능 산출물, `enissayosu/`·`minacalc/` 빌드에 안 쓰이는 외부 저장소 복사본 제외 — `osu/`·`sunnyosu/`는 유지). 초기 커밋 + gitignore 패턴 수정 커밋
