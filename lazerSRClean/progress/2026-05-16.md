# 2026-05-16

## Launcher 초기 생성

- `lazerSRClean/LazerSR.Launcher/` 프로젝트 생성
- `LazerSR.Launcher.csproj` 작성 (net8.0-windows, WPF, SingleFile, ExcludeHookDeps MSBuild 타겟 포함)
- `App.xaml` / `App.xaml.cs` 생성
- `MainWindow.xaml` — 제목 / osu경로+Browse / Launch+PID텍스트 / Features 헤더(빈 영역) 레이아웃
- `MainWindow.xaml.cs` — Launch, Browse, PipeClient 연결 로직 (기능 토글 없음)
- `Configuration/` — `InstallPathProvider`, `LauncherSettings`, `LauncherSettingsStore`, `LazerInstallLocation`
- `Ipc/PipeClient.cs` — Named Pipe 클라이언트 (기존 코드 그대로)
- `dotnet build` 통과 (경고 0, 오류 0)

## sunnySR 계산기 내장 + 런처 디버그 표시

- `LazerSR.SunnyCalculator/Difficulty/MathUtils/LegacySortHelper.cs` 추가 (누락 파일)
- `Difficulty/Skills/Strain.cs` `ObjectDifficulties` → `ObjectStrains` 수정 (osu! base class 호환)
- `LazerSR.Hook/LazerSR.Hook.csproj` 생성
- `LazerSR.Hook/StartupHook.cs`, `HookBootstrap.cs`, `DependencyResolver.cs`, `Patcher.cs`, `AccessHelper.cs`, `SunnyState.cs` 생성
- `LazerSR.Hook/Ipc/PipeServer.cs` 생성 (`BroadcastAsync` 포함)
- `LazerSR.Hook/Patches/DifficultyDisplayPatch.cs` 생성 (BeatmapTitleWedge.LoadComplete 패치, sunnySR 계산 + 브로드캐스트)
- `LazerSR.Launcher/Ipc/PipeClient.cs` 수정 (reader 유지, `MessageReceived` 이벤트 추가)
- `LazerSR.Launcher/MainWindow.xaml` 수정 (sunnySR TextBlock 추가)
- `LazerSR.Launcher/MainWindow.xaml.cs` 수정 (`sunnysr:` 메시지 처리)
- `dotnet build` 전체 통과 (경고 0, 오류 0)

## 런처 패키징

- `LazerSR.Launcher.csproj`에 `<ProjectReference Include="..\LazerSR.Hook\LazerSR.Hook.csproj" />` 추가 (publish 출력에 Hook DLL 포함 위해 필수)
- `dotnet publish -c Release` 통과
- 출력: `LazerSR.Launcher.exe` (SingleFile ~226MB) + `LazerSR.Hook.dll`, `LazerSR.SunnyCalculator.dll`, HarmonyX/MonoMod/Mono.Cecil DLL 루즈 파일

## 1.1 sunnySR pill 인게임 표시

- `DifficultyDisplayPatch.cs` 재작성 — 패치 대상 `BeatmapTitleWedge.LoadComplete` → `BeatmapTitleWedge+DifficultyDisplay.LoadComplete`
- GridContainer ColumnDimensions + Content 동적 확장으로 sunnySR `StarRatingDisplay` 기존 SR pill 옆(같은 줄)에 추가
- `ConditionalWeakTable` 퍼인스턴스 상태 + `WeakReference` 클로저로 메모리 안전 보장
- 파이프 브로드캐스트 동일 패치로 통합 (별도 BeatmapTitleWedge 패치 제거)
- `dotnet build` 경고 0 오류 0, `dotnet publish` 성공

## 세션 관리 파일 정비

- `progress/2026-05-16.md` 생성 — 진행상황 로그 시작
- `start.md` 생성 — 세션 시작 시 필독 목록 (CLAUDE.md, 설계원칙 4개, 최신 로그, 참고 코드 위치)
- `CLAUDE.md` 갱신 — 세션 시작 규칙 및 진행상황 로그 규칙 추가
