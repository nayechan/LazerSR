# 2026-06-04

- `Drawables/StrainAreaGraph.cs` 수정: padX 6→0 (허니스팟 좌표계와 막대 좌표계 불일치 수정)
- `SunnyManiaDifficultyCalculator.cs` 수정: GetStrainTimeline 반환값 dho.StartTime→dho.BaseObject.StartTime (배속 시 마스크·구간타이머 타이밍 오류 수정)
- dotnet publish Release 완료
- `DifficultyDisplayPatch.GetDominant()` 수정: TEC 비교 가중치 0.9 적용 (표시값은 원본 유지)
- `StrainGraphWidget` 수정: ModSettingChangeTracker 추가 + 0.5초 debounce (scheduleRecalculate)
- dotnet publish Release 완료
- `SunnyRunner.CalculateTimed()` 추가 (SunnyCalculator)
- `SunnyPPWidget` 신규 생성 (스킨 위젯, sunnySR 기반 실시간 pp)
- `SkinWidgetRegistrarPatch` 에 SunnyPPWidget 등록
- dotnet publish Release 완료
- `SunnyPPWidget` 수정: "sunny pp" 라벨 제거, `글꼴(Typeface)` 설정 추가
- `Calculators/PPCurveAnalyzer.cs` 신규: 기존SR 기반 PP 커브 → 기울기 세그먼트 → honey spot 2 시간 구간
- `Drawables/StrainAreaGraph.cs` 수정: `honeySpot2Overlays` 추가, `Update()` 시그니처 변경
- `Widgets/StrainGraphWidget.cs` 수정: `HoneySpot2Threshold` 설정, `BeatmapDifficultyCache` DI, 비동기 PP 커브 분석 (빨간 그라데이션)
- dotnet publish Release 완료
- `PPCurveAnalyzer` + `StrainGraphWidget` 수정: `PP 가중치 지수` 슬라이더 추가 (0~4, 기본 1.5) — `(pp/ppMax)^k` 배수 적용
- dotnet publish Release 완료
- `StrainGraphWidget` 수정: timedAttrs 400ms 윈도우 다운샘플 후 PPCurveAnalyzer 호출 (병목#2 감소)
- `PPCurveAnalyzer` 수정: ScoreInfo MaxCombo `i+1` → `Attributes.MaxCombo` (다운샘플 후 정확도 유지)
- dotnet publish Release 완료
- honey spot 1(금색 그라데이션) 완전 제거: `StrainGraphData.HoneySpots`, `SunnyStrainGraph.FindHoneySpots` 및 관련 메서드·상수, `StrainAreaGraph.honeyOverlays`, `StrainGraphWidget.ShowHoney` 삭제
- honey spot 2(빨강) 색상 → 금색으로 변경 (PP 커브 기반 꿀스팟으로 대체)
- dotnet publish Release 완료, installer v4.0.0 생성
