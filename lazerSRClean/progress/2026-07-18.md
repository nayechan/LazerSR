# 2026-07-18

- osu!lazer 스킨 로드/저장 구조 코드 감사 (`SkinManager`/`SkinImporter`/`Skin`/`LegacySkin`) — 해시 기반 파일 저장 + realm 메타데이터, 스킨 전환 시 `Skin` 인스턴스 재생성 확인. "리로드 단축키"는 없고 `Ctrl+Shift+E/T/R`(이전/다음/랜덤 스킨)만 존재함을 확인
- `LegacyManiaSkinConfigurationLookups` 44개 항목 분류 — 파일/이미지 lookup(19개) vs 실제 수치·색상·동작 값(23개) vs Argon 전용 미구현 항목(2개, `StagePaddingTop/Bottom`) 구분
- `ScorePosition`이 점수 HUD 위치가 아니라 판정 팝업(300/MISS) 위치임을 `LegacyManiaJudgementPiece.cs` 확인으로 정정
- 신규 위젯 `ManiaPositionAdjustWidget` 구현 (인게임 HUD, mania 전용) — 체크박스로 접기/펼치기, HitPosition/ScorePosition 각각 -1/+1 버튼 + 현재값 표시
- 신규 공용 헬퍼 `ManiaSkinPositionAccess.cs` 추가 — `LegacySkin.ManiaConfigurations`(internal) 리플렉션 접근 + `SkinManager.SourceChanged`(이벤트 백킹필드) 리플렉션 발화로 값 변경을 즉시 화면에 반영. **세션 한정** — skin.ini에 저장하지 않음, 재시작하면 리셋
- `SkinWidgetRegistrarPatch.LazerSRWidgets`에 등록
- `LazerSR.Hook` 빌드 검증 — 오류 0개 (기존과 동일한 OFSG001 경고만 발생)
- 사용자 실기 테스트: 게임 실행/플레이는 정상이나 **스킨 에디터 진입 시 osu 즉사** 재현됨
- 원인 후보 1: `ManiaPositionAdjustWidget` 자체 버그. 원인 후보 2: 직전에 "위젯이 목록에 안 보인다"는 문제 진단 중 `SkinWidgetRegistrarPatch`의 `ruleset.ShortName != "mania"` 필터를 제거해서, 기존 6개 위젯(Dan/MSD/StrainGraph/ReplayCompare/SectionTimer/SunnyPP)까지 처음으로 non-mania/글로벌 스킨 에디터 섹션에 노출되기 시작함 — 즉사가 신규 위젯이 아니라 이 노출 확대 때문일 가능성도 있음. 실제로는 나중에 "안 보인 이유"가 필터가 아니라 Launcher가 구버전 Hook.dll을 물고 있었던 것으로 판명됨(§Launcher 재빌드 참고) — 필터 제거는 결과적으로 불필요했을 수 있음
- 진단용 임시 로깅 추가 (**임시, 원인 파악 후 되돌릴 것**):
  - `HookLog.cs` — 기본 no-op에서 바탕화면 `lazersr-debug.log` 파일 기록으로 임시 전환 (즉시 flush, try/catch로 로깅 자체는 절대 안 죽게)
  - `ManiaPositionAdjustWidget` 및 3개 nested 클래스(`PositionField`/`StepButton`/`SquareCheckbox`) 생성자·`load()`·`LoadComplete()`·각 메서드에 진입/종료 체크포인트 로그 추가, `load()`/`LoadComplete()`는 try/catch로 예외 스택트레이스까지 기록 후 rethrow
  - `SkinWidgetRegistrarPatch.Postfix` — 호출될 때마다 `ruleset` 값과 추가되는 위젯 타입 목록 기록 (위 원인후보 2번 검증용)
- `LazerSR.Launcher` 재빌드 완료(로깅 포함), 오류 0개 — 사용자가 실행 후 크래시 재현하고 `lazersr-debug.log` 보내주기로 함
- 로그 분석: `ManiaPositionAdjustWidget`은 생성자→`load()`→`LoadComplete()`→강제 `DrawSize` 계산→`Update()` 첫 프레임까지 매번 전부 정상 종료 — 우리 위젯의 예외/레이아웃 순환참조 가설, MinaCalc 네이티브 호출 가설(`MsdSkillsetWidget`의 `load()`도 도달 전에 죽음 확인) 전부 로그로 배제
- 사용자 지적으로 기존 5개 위젯(Dan/StrainGraph/SunnyPP/ReplayCompare/SectionTimer)에 달았던 진단 로그는 전부 원복 (검증된 기존 코드를 계속 건드리는 게 비효율적이라는 판단)
- **이진 탐색으로 등록 여부 → 위젯 내용물 → 리플렉션 → UI 구조 순으로 격리**: 미등록(정상) → 등록+최소 스텁(정상) → 스텁+`ManiaSkinPositionAccess` 리플렉션 호출(정상, 리플렉션 완전 결백 확정) → 스텁+원본과 동일한 4단 중첩 FillFlowContainer 구조(**즉사**) → 깊이만 4단 유지 단순 콘텐츠(정상) → 가로 FillFlow에 박스+고정폭 텍스트(**즉사**, 박스 제거해도 즉사) → 텍스트 3개 Anchor 없음(정상) → 텍스트 하나에 `Anchor.Centre`+고정 `Width`만 추가(**즉사**)
- **근본 원인 확정**: `FillFlowContainer`(`AutoSizeAxes.Both`)에 직접 들어가는 자식 Drawable에 `Anchor.Centre`+한쪽 축만 고정 크기(다른 축은 auto)를 동시에 주면, 이게 3~4단 이상 중첩된 `AutoSizeAxes.Both` FillFlowContainer 체인 안에 있을 때 osu!가 예외 로그 하나 없이 즉사함(스택오버플로우 추정, C# try/catch로 못 잡음). 서브클래싱 여부, 인스턴스 개수, 커스텀 폰트, Box 혼합 여부는 전부 무관 — 오직 이 조합 하나가 트리거. `docs\guides\ui-patching.md` §6에 함정으로 기록
- **수정**: 문제의 값 텍스트(`PositionField`)를 양쪽 축 다 고정된 `Container`로 감싸고 그 안에서 `Anchor.Centre` 적용 — 이후 실기 테스트로 정상 동작 확인 (HitPosition/ScorePosition 버튼, 화면 갱신 전부 정상)
- 진단용 임시 코드 전부 원복: `HookLog.cs` no-op으로 복귀, `SkinWidgetRegistrarPatch`의 mania 전용 필터 복원(필터 제거는 애초에 불필요한 조치였음 — 실제 "안 보임" 원인은 Launcher가 구버전 Hook.dll을 물고 있었던 것)
- 디자인 다듬기: 체크박스 14×14→11×11 축소 + `CornerRadius=3` 모서리 처리, 접기/펼치기를 `body.AutoSizeAxes` 토글 대신 `body.Alpha`만 바꾸는 방식으로 변경 — 위젯 전체 바운딩 박스가 상태에 따라 변하지 않아 체크박스 화면 위치가 고정됨
- 최종 빌드 검증 — 오류 0개
- `docs\guides\ui-patching.md` §6에 이번 크래시 함정 기록 추가
- 신규 위젯 `ClassicAccuracyWidget` 구현 — 구 클라이언트(stable) 방식 정확도 표시. 300g 고정 16ms + `64/97/127/151/188 - 3*OD` 판정창, 분모 300 기준 정확도. 화면엔 `xx.xx%` 텍스트만, 설정은 글꼴 선택 하나뿐
- 신규 계산기 `Calculators\ClassicManiaAccuracy.cs` — OD 기반 판정 분류만 담당하는 순수 함수 (`Judge(absOffsetMs, od)` → 300/300/200/100/50/0)
- 구현 전 사전 조사로 osu!lazer 소스에서 두 가지 사실 확인: (1) `ManiaHitWindows`의 GREAT~MISS 공식이 요청받은 구클라 공식과 완전히 동일(`64/97/127/151/188-3*OD`), PERFECT만 lazer는 OD가변인데 요청은 고정 16ms. (2) `JudgementResult.TimeOffset`은 head/tail 구분 없이 항상 원본(raw) ms 오차 — `DrawableHoldNoteTail`이 판정 등급 계산에만 `/1.5`를 적용하고 저장값(`RawTime = Time.Current`)엔 반영 안 함
- 롱노트 head/tail은 `beatmap.HitObjects`에서 `Head/Tail → HoldNote` 매핑을 만들어 raw offset 산술평균으로 판정 1개만 집계 (`ReplayScoreTimeline.cs`와 동일한 매핑 패턴 재사용)
- `SkinWidgetRegistrarPatch.LazerSRWidgets`에 등록, Hook/Launcher 빌드 검증 — 오류 0개
- 사용자 실기 테스트 중 버그 발견: 리플레이 재생 시점을 앞뒤로 스크롤(되감기)하면 정확도가 꼬임
- 원인 조사: `ScoreProcessor.NewJudgement`만 구독하고 되감기 전용 이벤트 `JudgementReverted`(`JudgementProcessor.cs`)를 무시하고 있었음 — 되감았다가 다시 앞으로 재생하면 판정이 재발생해 누적값이 이중 가산되는 구조적 드리프트 버그. lazer 기본 `LegacyAccuracyCounter`는 개별 판정을 누적하지 않고 `ScoreProcessor.Accuracy` bindable을 구독만 해서 이 문제가 없음(그 bindable 자체가 apply/revert 대칭 상태머신) — 다만 우리는 커스텀 공식이라 그 bindable을 그대로 못 씀
- 추가 조사: 리플레이 1초/5초/10초/프레임 스킵 기능은 전부 `GameplayClockContainer.Seek()`로 귀결되며, 뒤로 스킵은 마우스 되감기와 완전히 동일한 코드 경로(`Playfield`의 `judgedEntries` LIFO 스택 되돌림)를 탐 — 별도 대응 불필요, `JudgementReverted` 처리 하나로 스킵까지 전부 커버됨을 확인
- `ClassicAccuracyWidget`에 `onJudgementReverted` 추가 — `onNewJudgement`와 정확히 대칭. tail 되돌림 시 콤바인에 쓰인 head offset을 보관해뒀던 `resolvedHead`에서 꺼내 numerator/noteCount를 원복하고 `pendingHead`에 복원(이어지는 head 되돌림·재정방향 재생과 자연히 맞아떨어짐). 일반 노트는 `result.TimeOffset`이 apply~revert 사이 안 바뀐다는 것을 코드로 확인했으므로 별도 저장 없이 그대로 재계산해서 뺌
- Hook/Launcher 재빌드 검증 — 오류 0개. 사용자 실기 테스트로 되감기/스킵 정상 동작 확인

---

- 결과창(4.2, `ExpandedPanelMiddleContentPatch`) sunnySR pill 버그 수정: 기존엔 `SunnyState.CurrentSr`(노래 선택/로딩 화면 전용 공유값)를 그대로 구독만 해서, 플레이 직후 자동 결과창 외엔(리더보드 클릭, 결과창 좌우 다른 점수 탐색) 항상 stale 값만 표시되던 문제. 각 패널의 `score`(private 필드, 리플렉션)에서 `BeatmapInfo`/`Ruleset`/`Mods`를 직접 꺼내 `SunnyRunner.Calculate`로 그 자리에서 재계산하도록 수정 — 공유 상태가 아닌 패널 전용 로컬 `Bindable<StarDifficulty>`로 바꿔 패널 간 값 오염 방지, 계산 완료 전엔 `Alpha=0`으로 숨김. mania 아닌 스코어는 pill 자체 생략
- 결과창에 클래식(구클라) 정확도 표시 추가: `ClassicAccuracyState.cs`(신규, `ScoreId`+`Accuracy` 단일 슬롯)를 만들어 `ClassicAccuracyWidget.updateDisplay()`가 100ms마다 갱신할 때 같이 발행하도록 수정. 결과창 4.2 패치가 패널의 `score.ID`와 `ClassicAccuracyState.ScoreId`가 일치할 때만 값을 표시하고, 그 외(리더보드 스코어, 다른 사람 점수로 이동 등 실제로 리플레이 재생을 통해 계산된 적 없는 경우)엔 `-` 표시. 기존 정확도(`AccuracyStatistic`) 바로 아래에 동일 폰트(`OsuFont.Torus.With(size:20, fixedWidth:true)` + `Spacing(-2,0)`)로 삽입
- 결과창에 sunnyPP 표시 추가: 동일한 매커니즘으로 `SunnyPPState.cs`(신규) + `SunnyPPWidget.UpdateDisplay()`에서 발행, `PerformanceStatistic`(PP) 아래에 `{pp}pp` 또는 `-` 표시. 두 통계 삽입 로직(StatisticDisplay 하위 FillFlowContainer 탐색 후 텍스트 append)이 완전히 동일해 `AppendStatisticLine` 공통 헬퍼로 통합
- Hook/Launcher 재빌드 검증 — 오류 0개. 사용자 실기 테스트로 세 가지(sunnySR pill 재계산, 클래식 정확도, sunnyPP) 정상 동작 확인
- `docs\end.md`에 Inno Setup Compiler(ISCC.exe) 실제 설치 경로(`C:\Users\shins\AppData\Local\Programs\Inno Setup 6\ISCC.exe`, PATH 미등록) 기록
- v5.0.0 배포: `dotnet publish -c Release` → `installer\LazerSRClean.iss` MyAppVersion 4.1.0→5.0.0 → ISCC 컴파일 → `installer\output\LazerSR-v5.0.0-Setup.exe` 생성 완료
