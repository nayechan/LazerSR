# 2026-05-18

- `AccessHelper.cs` — `FindFirstChildOfType` 헬퍼 추가 (DifficultyDisplayPatch에서 공유용으로 이동)
- `BeatmapMetadataDisplayPatch.cs` 신규 (4.1): `versionFlow` 필드 via reflection → `AddInternal`로 pill 삽입
- `ExpandedPanelMiddleContentPatch.cs` 신규 (4.2): `FindFirstChildOfType`로 StarRatingDisplay 탐색 → 부모 FillFlow에 pill 삽입
- `BeatmapAttributesDisplayPatch.cs` 신규 (2.1): `starRatingDisplay` 필드 via reflection → 부모 LeftContent에 pill 삽입
- `dotnet build` 통과 (경고 60개, 에러 0개)
- 4.1 크래시 수정: `versionFlow` (Vertical FillFlow)에 추가할 pill의 Anchor를 `CentreLeft→TopCentre`로 변경 (FillFlowContainer RelativeAnchorPosition 제약 위반)
- 2.1 순서 수정: `AddInternal` → `Insert(1, pill)` (기존SR → sunnySR → bpm 순서)
- 2.1 pill 미표시 수정: `typeof(CompositeDrawable)` → `parent.GetType()` (`Insert`는 `Container<T>`에 있어 CompositeDrawable에서 탐색 시 null 반환)
- mod 세팅 변경 시 sunnySR 갱신: `ModSettingChangeTracker` 도입, DT 속도 등 세부 조절에도 재계산 트리거
- `Widgets/HelloWorldWidget.cs` 신규: 스킨 위젯 테스트용 "Hello World" 텍스트 위젯
- `Patches/SkinWidgetRegistrarPatch.cs` 신규: `SerialisedDrawableInfo.GetAllAvailableDrawables` Postfix, global/mania 스킨에 `HelloWorldWidget` 등록
- `HelloWorldWidget.cs` 삭제
- `SunnyManiaDifficultyCalculator.cs` 신규: `GetStrainTimeline` (standalone, `GetObjectStrains()` 방식)
- `Data/StrainGraphData.cs` 신규
- `Calculators/SunnyStrainGraph.cs` 신규: 스무딩 + honey spot 탐지
- `Drawables/StrainAreaGraph.cs` 신규: GL 렌더러 기반 그래프 drawable
- `Widgets/StrainGraphWidget.cs` 신규: 스킨 위젯, gameplay 중 strain 그래프 + honey spot 표시
- `SkinWidgetRegistrarPatch.cs` 수정: `HelloWorldWidget` → `StrainGraphWidget`
- dotnet publish Release 완료
- `StrainGraphWidget.cs` 수정: song select 진행 바 추가 (`WorkingBeatmap.Track.CurrentTime` fallback)
- `StrainAreaGraph.cs` 수정: 높이 가중치 power curve γ=2.2 적용 (`MathF.Pow(x, 2.2f)`)
- `Patches/DifficultyIconTooltipPatch.cs` 신규: `DifficultyIconTooltip.SetContent` Postfix → 기존SR pill 아래에 sunnySR pill 삽입 (세로 FillFlow, Insert(2, pill)) ✅ 동작 확인
- dotnet publish Release 완료
- `Data/MsdData.cs` 신규: MSD 스킬셋 데이터 record
- `Calculators/MsdCalculator.cs` 신규: MinaCalc 505 P/Invoke, 전용 워커 스레드, latest-wins 메일박스
- `Drawables/MsdBarChart.cs` 신규: 8개 스킬셋 막대 그래프 GL 렌더러
- `Widgets/MsdSkillsetWidget.cs` 신규: 스킨 위젯, gameplay/song-select 양쪽 대응, 4K 전용
- `SkinWidgetRegistrarPatch.cs` 수정: MsdSkillsetWidget 등록 추가
- `Hook.csproj` / `Launcher.csproj` 수정: MinaCalc.dll 복사 항목 추가
- `MinaCalc.dll` Hook 프로젝트 루트에 배치
- dotnet build 에러 0개 확인
- `MsdBarChart.cs` 수정: FastCircle 셰이더 제거 → Box 드로어블 + CornerRadius=2 (2px 곡률), 하단 약어 레이블 추가 (OVR/STR/JS/HS/STA/JK/CJ/TEC)
- dotnet publish Release 완료

- `MinaCalc.dll` 재빌드 (minacalc_build2): vcvars64.bat + cmake NMake Release, `calc_timeline` export 확인
- `Data/SkillsetTimelineData.cs` 신규: Stream/Js/Hs/Jack/Cj/Tech 배열 + IntervalCount record
- `MsdCalculator.cs` 수정: `calc_timeline` P/Invoke 추가, `ComputeTimeline()` 내부 메서드, `LastTimeline` static volatile 프로퍼티 (RunCalc 호출 시 자동 갱신)
- dotnet publish Release 완료 (에러 0)
- `DifficultyDisplayPatch.cs` 수정: `ComputeDominant` → `GetDominant(MsdData)` 교체. sunny strain 80%↑ 구간 노트만 HashSet<int>으로 필터링 → `MsdCalculator.CalculateFiltered`로 해당 구간만 `calc_msd` 실행 → 지배 스킬셋 표시 (base_adj_diff 스케일 문제 해결)
- `MsdCalculator.cs` 수정: `MsdRequest`에 `HashSet<int>? KeepTimesMs` 추가, `Enqueue` 내부 메서드로 리팩토링, `CalculateFiltered` 공개 메서드 추가, `ConvertBeatmap`에 시간 필터 지원, 필터 있을 시 `LastTimeline` 갱신 생략
- dotnet publish Release 완료 (에러 0)
- `Calculators/DanCalculator.cs` 신규: backup DanCalculator.cs 이식 (sunnySR → 단 명칭/tier)
- `Calculators/PatternBpmCalculator.cs` 신규: IBeatmap 기반 패턴 BPM 계산 (STR/JS/HS/JK/CJ/TEC, 최대 BPM)
- `SunnyState.cs` 수정: `CurrentDominant` Bindable<string> 추가
- `DifficultyDisplayPatch.cs` 수정: dominant 약어를 `SunnyState.CurrentDominant`에 Schedule publish
- `Widgets/DanInfoWidget.cs` 신규: 스킨 위젯 "[단명] [tier] [BPM]BPM [약어]" 텍스트 표시
- `SkinWidgetRegistrarPatch.cs` 수정: DanInfoWidget 등록
- dotnet build 에러 0개 확인
- `DanInfoWidget.cs` 수정: 맵 변경 시 갱신 안 되는 버그 수정 (Recalculate 시작 시 CurrentDominant="" 즉시 초기화, 로컬 Bindable로 static 구독 누수 방지)
- `DanInfoWidget.cs` 수정: 출력 BPM에 mod rate 곱하기 (IApplicableToRate), raw BPM 캐시 + updateDisplay() 분리, ModSettingChangeTracker로 상세설정 변경 시에도 갱신
- dotnet publish Release 완료
