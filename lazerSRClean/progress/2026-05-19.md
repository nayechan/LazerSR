# 2026-05-19

- `Calculators/BestScoreFinder.cs` 신규: Realm 쿼리로 현재 맵+모드 기준 본인 최고점수 정확도 반환
- `DifficultyDisplayPatch.cs` 수정: Dependencies에서 RealmAccess/IAPIProvider 추출, `bestacc:{xx.xx%}` 브로드캐스트 추가
- `MainWindow.xaml` 수정: "Best Acc" 행 추가 (BestAccTextBlock)
- `MainWindow.xaml.cs` 수정: `bestacc:` 메시지 처리, 연결 해제 시 초기화
- dotnet build 에러 0 확인 (Hook + Launcher)
- `BestScoreFinder.cs` 수정: RealmUser.Username 필터를 Realm 쿼리에서 메모리 필터로 이동 (embedded object LINQ 번역 오류 수정)
- dotnet publish Release 완료
- `BestScoreFinder.cs` 수정: `FindBestReplayPath()` 추가 — .osr 절대경로 반환 (Storage.GetStorageForDirectory("files") + GetStoragePath())
- `Calculators/ReplayScoreTimeline.cs` 신규: .osr 파싱 + lazersunny calc_timeline_v2 C# 이식, LZMA는 SharpCompress reflection 호출
- `Patches/PlayerGameplayPatch.cs` 신규: Player.LoadComplete Postfix — 타임라인 계산 후 100ms 폴링으로 `replaycompare:{score}:{combo}` 브로드캐스트
- `MainWindow.xaml` 수정: "Replay" 행 추가, Height 315
- `MainWindow.xaml.cs` 수정: `replaycompare:` 처리
- dotnet publish Release 완료

- `ReplayScoreTimeline.cs` 수정: 단계별 진단 로그 추가 후 원인 확정
  - 원인: SharpCompress 설치 버전(0.47.0.0)의 `LzmaStream.Create` 시그니처가 `(byte[], Stream, long, long, bool)` — bool isLzma2 파라미터 필수
  - 수정: `GetMethod` 시그니처에 `typeof(bool)` 추가, invoke 시 `false` 전달
  - 진단 로그 제거
- dotnet publish Release 완료

- `Data/ReplayFrame.cs` 신규: (Time, Score, Combo, J320~JMiss) 구조체
- `Data/ReplayCompareState.cs` 신규: Timeline + ReplayDate 정적 공유 상태
- `Calculators/ReplayScoreTimeline.cs` 수정: 반환타입 ReplayFrame[], 판정별 누적 카운트, .osr 헤더에서 날짜 파싱
- `Patches/PlayerGameplayPatch.cs` 수정: ReplayCompareState 채우기
- `Widgets/ReplayCompareWidget.cs` 신규: ISerialisableDrawable, 설정 10개, 100ms 폴링 live vs replay 비교
- `Patches/SkinWidgetRegistrarPatch.cs` 수정: ReplayCompareWidget 등록
- dotnet publish Release 완료

- `Calculators/ReplayScoreTimeline.cs` 전면 리팩토링: 자체 hit window/점수 공식 제거, osu! lazer 컴포넌트 직접 활용
  - `ManiaScoreProcessor` 인스턴스 + `ApplyBeatmap` → 점수/콤보/누적 자동 계산 (V2 공식 일치, mod 자동 반영)
  - `hitObject.HitWindows.ResultFor(offset)` → 판정 (HR/DT/HT 등 mod 적용 완료된 윈도우 사용)
  - `OrderedHitPolicy` 로직 이식: 칼럼 큐 + (1) miss window 지남 auto-miss + (2) 다음 노트 startTime 넘으면 candidate 강제 Miss
  - HoldNote 처리: Head 매칭, Tail은 release 시 `RELEASE_WINDOW_LENIENCE=1.5` 적용 + head 미히트면 Meh 캡, Body는 tail.IsHit에 따라 IgnoreHit/ComboBreak
- dotnet publish Release 완료

- `Widgets/ReplayCompareWidget.cs` 수정: Acc 구현 — `CalcAcc()` 추가, 분모 305 고정, live/replay on-the-fly 계산
- `Widgets/ReplayCompareWidget.cs` 수정: PP 구현 — `PerformancePointsCounter` 패턴, `TimedDifficultyAttributes` 이진탐색, `ManiaPerformanceCalculator.Calculate()` live/replay 각각, `GameplayWorkingBeatmap` inner class 추가
- dotnet publish Release 완료

- `Widgets/ReplayCompareWidget.cs` 수정: `ShowDetails` 체크박스, 행 간격 MinValue -4, `Font` 글꼴 선택 (Venera/Torus/TorusAlternate/Inter)
- dotnet publish Release 완료

- `Calculators/BestScoreFinder.cs` 수정: Mirror 제외, NC↔DT/DC↔HT 같은 군 취급, 같은 군 다른 acronym은 `speed_change`만 비교
- `Calculators/ReplayScoreTimeline.cs` 수정: 날짜 버그 — `DateTime.FromFileTimeUtc` → `new DateTime(ticks, DateTimeKind.Utc)`
- dotnet publish Release 완료

- `Data/SectionInterval.cs` 신규: `record SectionInterval(StartMs, EndMs, IsHard)`
- `Data/SectionTimerState.cs` 신규: `PendingSections` / `ActiveSections` volatile 공유 상태
- `Calculators/SectionTimeline.cs` 신규: SmoothD → 감마(^3/2.2) → 추가 스무딩(σ=1500ms) → 75% 임계 → 4000ms 병합
- `Widgets/SectionTimerWidget.cs` 신규: ISerialisableDrawable, 100ms 폴링, 빨강(HARD)/흰색(REST) 남은시간 텍스트
- `SunnyStrainGraph.cs` 수정: `GaussianSmooth` private → internal
- `DifficultyDisplayPatch.cs` 수정: strain 계산 후 SectionTimeline.Calculate → PendingSections 저장
- `PlayerGameplayPatch.cs` 수정: Player.LoadComplete 시 ActiveSections 스냅샷
- `SkinWidgetRegistrarPatch.cs` 수정: SectionTimerWidget 등록
- dotnet publish Release 완료

- `Calculators/SectionTimeline.cs` 수정: σ=1500→1100ms, hardThresholdFraction/minSegmentMs 파라미터화
- `Widgets/SectionTimerWidget.cs` 수정: 내부 자체 계산 구조로 변경, 임계값(%) + 병합기준(ms) 슬라이더 설정 추가
- `DifficultyDisplayPatch.cs` 수정: SectionTimeline 호출에 named token 파라미터 적용
- dotnet publish Release 완료

- `HookLog.cs` 수정: Write() no-op으로 교체 (바탕화면 로그 파일 생성 제거)
- `MainWindow.xaml` 수정: Features 섹션(sunnySR/Dominant/Best Acc/Replay 행) 제거, Height 315→160
- `MainWindow.xaml.cs` 수정: StatusChanged/MessageReceived에서 제거된 TextBlock 참조 삭제
- dotnet publish Release 완료

- `installer/LazerSRClean.iss` 신규: Inno Setup 설치마법사 (AppId 동일, v3.0.0, lazerSRClean publish 경로)
- `installer/output/LazerSR-v3.0.0-Setup.exe` 생성 (182MB, ISCC 컴파일 완료)
