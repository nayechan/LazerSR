# 2026-07-17

- 안전 레드라인 재정의 논의 — "게임플레이 경로 절대 금지" → "서버 제출값 실제 조작 여부"가 유일한 기준으로 변경 (osu! 무결성 모델, tosu 등 비교 검토)
- `C:\dev\lazerSR` 전체 문서/코드 감사 — progress 로그를 절대 기준으로 재평가, 실제 패치 코드(`PlayerGameplayPatch.cs`, `ReplayScoreTimeline.cs`, `SkinWidgetRegistrarPatch.cs`, `DifficultyIconTooltipPatch.cs`, `ManiaDifficultyHitObject.cs`) 직접 읽어 검증
- 구 osu 소스(`C:\dev\lazerSR\osu\`) 삭제 → `C:\dev\ppy-osu-1164870`(신버전)로 교체. `sunnyosu\`는 원본 유지
- legacy 잔재 삭제: `temp\`(621MB), 루트 `installer\`(구v2.0.0), `.superpowers\`, `docs\superpowers\`(v1/v2 죽은 계획), `brainstorm-v2-notes.md`, `lazerSRClean\GridInspect\`, `inspect_grid.csx`
- `lazerSRClean\docs\` 재구성: `guides\architecture.md`/`safety.md`/`feature-development.md`/`ui-patching.md` 전면 재작성(실제 코드 기준, 구 문서의 "패치 2개뿐"/"feature enum IPC" 서술 오류 정정), `INDEX.md`/`end.md` 신규, `start.md` 이동, `sibling-clone-position-map.md`/`skin-widget-research.md` 이관+정정, `replay-compare-widget-plan.md`에 안전성 각주 추가
- `lazerSRClean\CLAUDE.md`, 루트 `C:\dev\lazerSR\CLAUDE.md` 대폭 축소 (포인터 위주)
- 신규 osu 소스로 전체 빌드 검증 (`LazerSR.Launcher` → `Hook` → `SunnyCalculator` → `osu.Game`/`osu.Game.Rulesets.Mania`) — 오류 0개, 경고 72개(전부 기존 것). 검증 후 `bin`/`obj` 삭제
- 자동 메모리 4개 파일 갱신 + `project_doc_reorg_2026-07-17.md` 신규 작성
- 2026-07-12 감사 결과를 개정된 `safety.md`/`architecture.md`/`ui-patching.md` 기준으로 재평가 — `PlayerGameplayPatch`/`ReplayScoreTimeline`/`ModUtils`/스킨 위젯의 라이브 상태 읽기/"`Select.*` 타겟 이탈" 지적 6건은 전부 구판 규칙 폐기로 해소 확인. `ManiaDifficultyHitObject` 상속 위험(안정성)과 `DifficultyIconTooltipPatch` static bool 버그(기능) 2건만 유효한 것으로 재분류
- `ManiaDifficultyHitObject : DifficultyHitObject` 상속 제거 (bug A/B와 동일한 osu! 업데이트 재발 위험 차단) — `LazerSR.SunnyCalculator` 6개 파일에서 `List<DifficultyHitObject>` → `List<ManiaDifficultyHitObject>` 교체, 이제 불필요해진 `Cast<ManiaDifficultyHitObject>()` 우회 제거
- `DifficultyIconTooltipPatch._pillInserted` static bool → `ConditionalWeakTable` 인스턴스별 추적으로 수정 (세션 최초 1회만 동작하던 버그)
- 빌드 검증 (`LazerSR.Launcher` 기준, 오류 0개)
- 신규 버그 확인 및 수정: 멀티플레이 큐잉 중 로컬 캐러셀에서 다른 맵 미리보기 → 게임 시작 시 로딩 화면 sunnySR pill이 미리보기 맵 값을 그대로 표시하던 문제. 원인: `SunnyState.CurrentSr`은 단일 전역 채널이고 쓰기는 `DifficultyDisplayPatch`(송 셀렉트) 하나뿐이었음. `BeatmapMetadataDisplayPatch`(4.1, 로딩 화면)에 자체 재계산 로직 추가로 수정, 사용자 실기 테스트로 확인
- `architecture.md`/`safety.md`/`sibling-clone-position-map.md`에 위 수정사항 반영 (알려진 결함 목록에서 제거, 패치 설명 갱신)
- 배포는 보류 — 추가 패치 예정, 전부 완료 후 한번에 배포하기로 함
